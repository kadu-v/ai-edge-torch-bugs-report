[project]
name = "ai-edge-torch-bugs-report"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch         == 2.5.1",
    "torchaudio    == 2.5.1",
    "torchvision   == 0.20.1",
    "ai-edge-torch == 0.4.0",
]
